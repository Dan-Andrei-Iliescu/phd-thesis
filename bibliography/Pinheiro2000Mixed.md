# Mixed-Effects Models in S and S-Plus

[Springer](https://link.springer.com/book/10.1007%2Fb98882#authorsandaffiliationsbook)

- Mixed-effects models contain fixed effects (parameters common to the whole dataset), error terms (parameters unique to each sample), and random effects (parameters common to samples within a group).
- Mixed-effects models with grouped data are also called *hierarchical* or *multilevel* models.

illustrate the fundamental problem with ignoring

Why it is important to estimate group effects:

> *The boxplots of the residuals from the fm1Rail.lm fit by rail number, displayed in Figure 1.2, illustrate the fundamental problem with ignoring the classification factor when modeling grouped data: the “group effects” are incorporated into the residuals (which, in this case, have identical signs for each rail), leading to an inflated estimate of the within-rail variability.*
# 2022-01-24

## Preparation

Terminology:

- inferred latent variables / variational latent variables / estimated latent variables / $u$ or $\hat{u}$
- $v$-probing / probing $\hat{v}$ for $v$
- ground-truth factors / data-generating factors / effects
- variational model / variational latent posterior


## Meeting

### Intro
- Clarify that that is a definition
- We have a dataset defined in such and such a way, and our goal is to find a representation
- A representation network is a function that maps this to that
- There is nothing in the VAE to induce disentanglement, ther
- evidence accumulation is more accurate but still tractable
- There are datasets where the conditional joint distribution is nearly independent, and a model that assumes independence structurally does just fine. However, in general, a full distribution is required.
- but there
- start with definition, jump to particular example and say it's particular example
- flipping back and forth between the probabilistic language and what the task is
- datasets with confounding all italicized
- state what the example is before talking about it: they introduce a dataset of ... where the aim is ...
- schools and exam scores are not a substantive analogy
- use an existing example
- find datasets modelled with that exact same models
- we propose a new model, which we call the CxVAE
- we design a novel regularization objective; our objective has the property that it encourages
- which we call
- start with "we show" - with takeaway
- Confounding: fundamental definition fits in the introduction

### Background
- background, related work, my approach
- background: we review the standard setup
- related work show that this definition is widely understood
- evaluation: datasets, metrics
- don't try to get people on your side before you give a definition
- you give a definition, give formula, then say why it's a sensible metric
- what are the metrics that other people have used
- are there still holes that I don't know how to evaluate formally
- don't say we show, 
- parallel coordinates plot, the coordinates are the 
- comparison that people want to make first is on the x axis
- explain the ablation study, what the labels are

### Degree of confounding
- when introducing a generative model, show the distribution, just show v(1) and v(2)
- be careful with should, it should be used only when expressing an expectation
- justify 
- is your experiment

Old points
- You have to do things people are interested in. Use the same dataset, the same problem, the same metrics
- What is the history of confounded variables? How have people dealt with confounded variables in the past? What has your reading about matrix completion methods revealed about dealing with confounded variables? You have to show your roots.
  - What if I show my roots in the disentanglement literature?
  - But you don't test on their problems or use their metrics